@model IEnumerable<JobPortal1.Models.JobApplyStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobApplyStatusName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobApplyStatusName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobApplyStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobApplyStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobApplyStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
