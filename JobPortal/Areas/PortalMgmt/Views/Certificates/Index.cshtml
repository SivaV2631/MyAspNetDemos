@model IEnumerable<JobPortal.Models.Certificate>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CertificateName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CertificateAuthority)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LevelCertification)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FromYear)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CertificateName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CertificateAuthority)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LevelCertification)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FromYear)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.EmailAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CertificateId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CertificateId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CertificateId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
