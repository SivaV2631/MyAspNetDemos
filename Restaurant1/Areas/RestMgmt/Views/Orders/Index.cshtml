@model IEnumerable<Restaurant1.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@


<div class="card-columns">
    @foreach (var item in Model) {
        <div class="card border-success">
            <div class="card-header">
                @Html.DisplayNameFor(modelItem => item.OrderId) : @item.OrderId
            </div>
            <div class="card-body">
                @Html.DisplayNameFor(modelItem => item.CustomerId) : @item.CustomerId
                <br />
                @Html.DisplayNameFor(modelItem => item.Customer) : @item.Customer
                <br />
                @Html.DisplayNameFor(modelItem => item.OrderName) : @item.OrderName
                 <br />
                @Html.DisplayNameFor(modelItem => item.OrderStatus) : @item.OrderStatus
                 <br />
                @Html.DisplayNameFor(modelItem => item.Items) : @item.Items


            </div>
            <div class="card-footer">
                <a class="btn btn-sm btn-success" asp-action="Edit" asp-route-id="@item.OrderId">Edit</a>
                <a class="btn btn-sm btn-outline-primary" asp-action="Details" asp-route-id="@item.OrderId">Details</a>
                <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.OrderId">Delete</a>
            </div>
        </div>
    }
</div>


<a asp-area="RestMgmt" asp-controller="Categories" asp-action="Index">List of Categories</a>
